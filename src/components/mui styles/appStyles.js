import { styled } from '@mui/system';
import HeaderBackground from '../../images/Header-Background.jpeg';

export const StyledApp = styled('div', {
  name: 'StyledApp',
})({
  backgroundColor: 'rgb(44, 4, 28, .8)',
  color: 'white',
  postition: 'relative',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
